<import src="../../templates/login-gate.wxml" />

<view class="page-shell">
  <view class="page {{isLoginRequired ? 'page-login' : ''}}">
    <block wx:if="{{isLoginRequired}}">
      <template is="login-gate" data="{{ loginGate: loginGate, isAuthorizingLogin: isAuthorizingLogin }}" />
    </block>

    <block wx:elif="{{!isProfileReady}}">
      <view class="page-body">
        <view class="state-panel">
          <view class="state-loading" aria-label="加载中">
            <view class="state-loading-dot state-loading-dot-1"></view>
            <view class="state-loading-dot state-loading-dot-2"></view>
            <view class="state-loading-dot state-loading-dot-3"></view>
          </view>
          <view class="state-title">正在加载账号信息</view>
          <view class="state-text">请稍候，系统正在同步你的资料。</view>
        </view>
      </view>
    </block>

    <block wx:else>
      <view class="page-body profile-page-body">
      <view class="hero-card hero-card-profile hero-card-compact">
        <view class="profile-glow profile-glow-a"></view>
        <view class="profile-glow profile-glow-b"></view>

        <view class="hero-overline">账号中心</view>
        <view class="profile-top">
          <button class="profile-avatar-wrap" open-type="chooseAvatar" bindchooseavatar="onChooseAvatar">
            <view class="profile-avatar-inner">
              <image wx:if="{{avatarUrl}}" class="profile-avatar-image" src="{{avatarUrl}}" mode="aspectFill" />
              <view wx:else class="profile-avatar-default">微</view>
            </view>
          </button>
          <view class="profile-info">
            <view class="profile-nick-field">
              <input
                class="profile-nick-input"
                type="nickname"
                placeholder="请输入昵称"
                placeholder-class="profile-nick-placeholder"
                value="{{draftNickName || nickName}}"
                bindinput="onNickNameInput"
                bindblur="onNickNameConfirm"
                bindconfirm="onNickNameConfirm"
              />
            </view>
            <view class="hero-subtitle">{{isSubmittingProfile ? '保存中，请稍候...' : '点头像同步微信头像，修改昵称后会自动保存。'}}</view>
          </view>
        </view>
        <view wx:if="{{showProfileSyncPrompt}}" class="profile-sync-notice">
          <view class="profile-sync-copy">
            <view class="profile-sync-title">补全微信资料</view>
            <view class="profile-sync-desc">点头像同步微信头像，修改昵称后会自动保存。</view>
          </view>
        </view>
      </view>

      <view class="card stack-lg">
        <view class="section-header">
          <view class="section-title">常用操作</view>
          <view class="section-meta">快捷入口</view>
        </view>

        <view class="menu-item" bindtap="goToBind">
          <view class="menu-icon">绑</view>
          <view class="menu-text-group">
            <view class="menu-text">绑定新设备</view>
            <view class="menu-desc">添加 Windows 客户端</view>
          </view>
          <view class="menu-arrow">›</view>
        </view>
        <view class="menu-item" bindtap="goToRecords">
          <view class="menu-icon">记</view>
          <view class="menu-text-group">
            <view class="menu-text">发送记录</view>
            <view class="menu-desc">查看提醒发送状态</view>
          </view>
          <view class="menu-arrow">›</view>
        </view>
        <view class="button-secondary stack-lg profile-logout-button" bindtap="logout">注销当前登录</view>
      </view>

      <view class="card stack-lg">
        <view class="section-header">
          <view class="section-title">联系开发者</view>
          <view class="section-meta">点击复制</view>
        </view>
        <view class="version-row contact-row" bindtap="copyWechat">
          <view class="version-label">💬 微信</view>
          <view class="version-value">Y840013505</view>
        </view>
        <view class="version-row contact-row" bindtap="copyEmail">
          <view class="version-label">📧 邮箱</view>
          <view class="version-value">840013505@qq.com</view>
        </view>
      </view>

      <view class="card stack-lg">
        <view class="section-header">
          <view class="section-title">版本信息</view>
          <view class="section-meta">稳定运行</view>
        </view>
        <view class="version-row">
          <view class="version-label">当前版本</view>
          <view class="version-value">0.1.0</view>
        </view>
      </view>
      </view>
    </block>
  </view>
</view>
